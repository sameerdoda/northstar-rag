version: '3.8'

services:
  db:
    image: postgres:15
    container_name: northstar-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: northstar
    ports:
      - "5432:5432"
